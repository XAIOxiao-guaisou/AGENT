# ==================== 中文版 ====================

# 环境变量配置示例
# 复制此文件为 .env 并填入您的配置

# DeepSeek API 密钥 (必需)
# 从 https://platform.deepseek.com/ 获取
DEEPSEEK_API_KEY=your_api_key_here

# AI 模式选择 (可选)
# 可选值: 
#   - executor: 执行模式 (默认) - 完整实现代码
#   - auditor: 审计模式 - 仅审查逻辑,不修改代码
#   - reviewer: 评审模式 - 提供代码质量建议
# 默认: executor
ACTIVE_MODE=executor

# 温度参数 (可选)
# 控制 AI 输出的随机性
# 范围: 0.0 (确定性) - 1.0 (创造性)
# 默认: 0.0
TEMPERATURE=0.0

# 最大 Token 数 (可选)
# 控制 AI 响应的最大长度
# 默认: 4096
MAX_TOKENS=4096

# 重试限制 (可选)
# 同一文件连续失败多少次后进入手动模式
# 默认: 3
RETRY_LIMIT=3

# ==================== English Version ====================

# Environment Variables Configuration Example
# Copy this file to .env and fill in your configuration

# DeepSeek API Key (Required)
# Get it from https://platform.deepseek.com/
DEEPSEEK_API_KEY=your_api_key_here

# AI Mode Selection (Optional)
# Available values:
#   - executor: Execution mode (default) - Full code implementation
#   - auditor: Audit mode - Only review logic, no code modification
#   - reviewer: Review mode - Provide code quality suggestions
# Default: executor
ACTIVE_MODE=executor

# Temperature Parameter (Optional)
# Controls randomness of AI output
# Range: 0.0 (deterministic) - 1.0 (creative)
# Default: 0.0
TEMPERATURE=0.0

# Maximum Tokens (Optional)
# Controls maximum length of AI response
# Default: 4096
MAX_TOKENS=4096

# Retry Limit (Optional)
# How many consecutive failures before entering manual mode
# Default: 3
RETRY_LIMIT=3
